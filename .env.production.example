# Environment Variables for Netlify Production Deployment
# Copy these to Netlify Dashboard → Site settings → Environment variables

# MongoDB Configuration
MONGODB_URI=mongodb+srv://himani:ems@ems.z3zxn2h.mongodb.net/?retryWrites=true&w=majority&appName=EMS

# JWT Secret - CHANGE THIS TO A STRONG RANDOM STRING!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_STRING_IN_PRODUCTION

# Node Environment
NODE_ENV=production

# API Base URL - Update with your actual Netlify URL after deployment
NEXT_PUBLIC_API_BASE=https://YOUR-SITE-NAME.netlify.app

# Optional: If you want to use mock data (set to 0 for production)
NEXT_PUBLIC_USE_MOCK=0
